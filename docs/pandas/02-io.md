# 数据导入与导出

> 对应代码: [02_io.py](file:///d:/Nayey/Code/NayukiChiba/Machine-Learning-Algorithms/Basic/Pandas/02_io.py)

## 学习目标

- 掌握 CSV、Excel、JSON 等格式的读写
- 了解常用读取参数

## 读取数据

### CSV 文件

```python
df = pd.read_csv('file.csv')
df = pd.read_csv('file.csv', encoding='utf-8', sep=',')
```

常用参数：

| 参数       | 说明       |
| ---------- | ---------- |
| `sep`      | 分隔符     |
| `header`   | 表头行号   |
| `names`    | 自定义列名 |
| `usecols`  | 读取指定列 |
| `nrows`    | 读取行数   |
| `encoding` | 编码格式   |

### Excel 文件

```python
df = pd.read_excel('file.xlsx', sheet_name='Sheet1')
```

### JSON 文件

```python
df = pd.read_json('file.json')
```

## 导出数据

```python
df.to_csv('output.csv', index=False)
df.to_excel('output.xlsx', sheet_name='Sheet1')
df.to_json('output.json', orient='records')
```

## 练习

```bash
python Basic/Pandas/02_io.py
```
